<%= form_with(model: shelter) do |form| %>
  <% if shelter.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shelter.errors.count, "error") %> prohibited this shelter from being saved:</h2>

      <ul>
        <% shelter.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :location_address, style: "display: block" %>
    <%= form.text_area :location_address %>
  </div>

  <div>
    <%= form.label :contact_info, style: "display: block" %>
    <%= form.text_area :contact_info %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
